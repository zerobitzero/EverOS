CONV_BOUNDARY_DETECTION_PROMPT = """
### 核心任务
作为一名对话分析专家，你需要判断新传入的消息是否为一个已有对话“情节”的自然结尾。目标是将连续的对话流分割成有意义的、可独立记忆的片段（MemCell）。你的核心原则是 **“默认合并，谨慎切分”**。

### 对话上下文
**已有对话历史:**
```
{conversation_history}
```

**与上一条消息的时间间隔:**
`{time_gap_info}`

**新传入的消息:**
```
{new_messages}
```

### 决策变量详解
你需要输出三个关键决策变量：`should_end`, `should_wait`, 和 `topic_summary`。

1.  **`should_end` (结束当前情节):**
    -   **何时设为 `true`?** 仅当新消息明确开启一个与之前历史无关的新主题时。一个用于总结或收尾当前主题的消息（如“迁移完成了”）应被归入当前情节，而不是作为新情节的开始。
    -   **触发场景示例:**
        -   **跨天强制切分:** 只要新消息与上一条消息的日期不同（例如，从“昨天”到“今天”），就必须切分。这是最高优先级的规则。
        -   **主题切换:** 对话从“讨论项目A的技术细节”突然转向“周末去哪里玩”。
        -   **任务完成并开启新篇:** 一个任务的收尾消息（如“仓库迁移好了”）属于该任务的情节。只有当**下一条**消息开启了完全不相关的新话题时，才在那条新消息处切分。
        -   **长时间中断后开启新话题:** 时间间隔超过4小时，且新消息内容与历史对话无明显关联。


2.  **`should_wait` (等待更多信息):**
    -   **何时设为 `true`?** 当新消息**信息量不足**，无法判断其是否延续当前话题时。这是一个**安全选项**，用于避免在上下文不充分时草率地分割或合并对话。
    -   **此为默认行为，在以下情况必须设为 `true`:**
        -   **非文本消息:** 新消息仅为 `[图片]`, `[视频]`, `[文件]` 等占位符，没有附带任何可用于判断意图的文字。
        -   **无明确意图的短回复:** 新消息是极为简短的回应，如“好的”, “嗯”, “收到”, “lol”, “😂”。
        -   **系统或非对话消息:** 新消息是系统通知（如邀请、入群、退群）、转账提示等。**这类消息本身不提供足够信息来判断情节边界，必须等待后续的人类消息再做决策。**
        -   **不确定的中间状态:** 时间间隔在30分钟到4小时之间，且新消息内容模糊，既不像强延续，也不像强开启。

3.  **`topic_summary` (情节主题总结):**
    -   **何时生成?** 仅在 `should_end` 被设为 `true` 时。
    -   **内容要求:** 用一句话精准、客观地概括**即将结束的这个情节**的核心内容。例如：“确定项目A的最终技术方案”或“讨论并确认了周末的团队活动安排”。

### 决策指导原则
- **内容优先于形式:** 优先保证每个情节都包含一个完整、有价值的核心信息。不要被简单的问候语（“你好”）或结束语（“再见”）迷惑，它们应被归入其所服务的主要情节中。
- **合并是默认倾向:** 如果不确定，倾向于不切分 (`should_end: false`)。只有在看到明确的切分信号时才进行分割。
- **关注因果与流程连贯性:** 如果新消息是前序动作（如建群、添加成员）的直接目标或结果（如在群里发布第一条项目指令），应将它们合并成一个完整的情节。不要在连续的流程中间断开。
- **`should_end` 与 `should_wait` 互斥:** 如果 `should_end` 是 `true`，`should_wait` 必须是 `false`。如果 `should_wait` 是 `true`，`should_end` 必须是 `false`。
- **考虑上下文连贯性:** 将那些为了同一个小目标服务的、连续的、短时间内的“来回”对话视为一个整体。
- **系统消息的上下文相关性:** 不要将系统消息（如“[用户X]加入了群聊”）默认为话题切换的信号。它的意义由下一条人类消息决定。例如，如果下一条消息是“欢迎！我们刚才正讨论到...”，则应合并；如果下一条消息开启了全新话题，则可考虑切分。

### 输出格式
请严格按照以下JSON格式返回你的分析结果：
```json
{{
    "reasoning": "一句话解释你为什么做出 should_end 和 should_wait 的决策。",
    "should_end": boolean,
    "should_wait": boolean,
    "confidence": float,
    "topic_summary": "仅在 should_end 为 true 时填写。总结已结束情节的主题，否则为空字符串。"
}}
```"""

CONV_BATCH_BOUNDARY_DETECTION_PROMPT = """
作为一名对话分析专家，你需要在一段连续的对话中找出所有自然的"情节边界"，将对话流分割成有意义的、可独立记忆的片段（MemCell）。核心原则：**默认合并，谨慎切分**。

### 输入格式
以下是按时间顺序排列的完整对话记录，每条消息前标有编号和时间戳：

```
{messages}
```

### 何时切分

仅当出现以下**明确信号**时才在对应消息序号处添加边界：
- **跨天强制切分（最高优先级）：** 相邻消息日期不同，必须在日期变更处切分。
- **主题切换：** 对话从一个具体话题突然转向完全无关的新话题（如"项目架构"→"周末计划"）。
- **任务完结并开启新篇：** 收尾消息（如"仓库迁移好了"）归入当前情节；只有**下一条**消息开启全新话题时才切分。
- **长时间中断后开启新话题：** 时间间隔超过4小时，且新消息与历史对话无明显关联。

**不应切分：**
- 问候语、结束语（"再见"、"谢谢"）归入其所服务的主要情节
- 过渡短语（"对了"、"顺便说一下"）通常延续当前情节
- 短暂停顿（< 4小时）后继续同一话题

### `should_wait`
当**最后一段对话**信息量不足，无法判断情节归属时设为 `true`：
- **非文本消息：** 仅有媒体占位符（`[图片]`、`[视频]`、`[文件]`）且无文字说明
- **无意图短回复：** 极简回应（"好的"、"嗯"、"收到"、"😂"）
- **系统或非对话消息：** 系统通知（入群、退群、转账提示等）本身无法判断情节边界，必须等待后续人类消息再决策
- **内容模糊的中间状态：** 时间间隔在30分钟到4小时之间，且内容模糊，既不像强延续，也不像强开启

### 决策指导原则
- **合并为默认：** 不确定时倾向不切分，只有出现明确切分信号才分割
- **内容优先：** 问候语、结束语归入其服务的主要情节，不单独成片
- **流程连贯性：** 为同一目标服务的连续动作（如建群→发第一条指令）合并为一个情节
- **系统消息不触发切分：** 系统消息（如"[用户X]加入群聊"）的情节归属由紧随其后的人类消息决定

### 示例

**示例1——有一个边界：**
输入消息：
```
[1] [2024-03-10 09:00:00+00:00] 小明: 登录功能有个bug，帮我看看
[2] [2024-03-10 09:01:00+00:00] 小红: 好的，我看下日志
[3] [2024-03-10 09:05:00+00:00] 小红: 找到了，AuthService第42行空指针
[4] [2024-03-10 09:06:00+00:00] 小明: 修好了，谢谢！
[5] [2024-03-11 10:00:00+00:00] 小明: 今天中午有空吃饭吗？
[6] [2024-03-11 10:01:00+00:00] 小红: 可以，12:30？
```
输出：
```json
{{
    "reasoning": "消息1-4是一个完整的bug修复情节；消息5跨天且话题切换为午餐，应在此处切分。",
    "boundaries": [4],
    "should_wait": false
}}
```

**示例2——无边界：**
输入消息：
```
[1] [2024-03-10 14:00:00+08:00] 小明: Q2路线图进展怎么样？
[2] [2024-03-10 14:02:00+08:00] 小红: 完成了60%，还需要确定API规范。
[3] [2024-03-10 14:10:00+08:00] 小明: 好，明天我们一起过一遍规范。
```
输出：
```json
{{
    "reasoning": "全部消息都属于同一个Q2路线图讨论，没有话题切换。",
    "boundaries": [],
    "should_wait": false
}}
```

### 输出格式
请严格按照以下JSON格式返回：
```json
{{
    "reasoning": "<一句话说明所有边界决策的理由>",
    "boundaries": [<切分点消息编号，1起始的整数>],
    "should_wait": <boolean，描述最后一段消息是否信息量不足>
}}
```

**`boundaries: []` 时表示所有消息属于同一情节，不切分。**
"""

CONV_SUMMARY_PROMPT = """
你是一位专业的对话总结师。请根据以下对话内容，用一句话客观、精炼地总结其核心主题。
"""
